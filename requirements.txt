# Runtime dependencies for the WhatsApp Chat Exporter (local).
#
# Versions are pinned to the set verified in issue #13 (T4.0): the committed
# crypt15 fixture (tests/fixtures/msgstore.db.crypt15) decrypts and exports
# with exactly these versions. pycryptodome + javaobj-py3 are the crypt15
# decryption extras that whatsapp-chat-exporter needs but does not pull in
# automatically.

whatsapp-chat-exporter==0.12.1
pycryptodome==3.23.0
javaobj-py3==0.5.0
